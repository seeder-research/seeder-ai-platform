# FEYNMAN_IP is used by docker-compose.yml in this same directory to
# substitute into `ports:` host-IP bindings, which require a literal IP.
FEYNMAN_IP=<replace-with-actual-ip>
POSTGRES_PASSWORD=<secret>
